import Foundation

enum ScreenTimeConfig {
    static let appGroupIdentifier = "group.com.parentlock.parentlock"
    static let monitoringName = "ParentLockDeviceActivity"
    static let shieldConfigurationBundleIdentifier =
        "com.parentlock.parentlock.shield-configuration"
}
